/**
 * Base atoms for TaskTrove core state management
 *
 * This file previously served as a re-export hub but has been simplified
 * to avoid re-export patterns. Import directly from source files instead.
 */
