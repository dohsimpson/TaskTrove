{
  "name": "@tasktrove/parser",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsgo --project tsconfig.json"
  },
  "dependencies": {
    "@tasktrove/types": "workspace:*",
    "date-fns": "catalog:"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@repo/vitest-config": "workspace:*",
    "vitest": "catalog:"
  }
}
